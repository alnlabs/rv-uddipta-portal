{
  "name": "rv-uddipta-server",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "seed": "node -e \"const fs=require('fs');const p=require('path').join(__dirname,'data');fs.rmSync(p,{recursive:true,force:true});require('./db').ensureDb();console.log('Reseeded',require('./db').ensureDb().prepare('SELECT COUNT(*) c FROM flats').get().c,'flats');\""
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
